const Technologies = ({ name }) => {
    return (
      <span className="text-4xl background-animate bg-gradient-to-r from-blue-500 to to-pink-500 rounded-sm w-10 h-10 flex items-center justify-center">
        {name}
      </span>
    );
  };
  
  export default Technologies;